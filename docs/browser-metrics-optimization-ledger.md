# Browser Metrics Optimization Ledger

This ledger records each browser-metric optimization attempt so every change is
visible by commit, benchmark evidence, and outcome. The target is to make Tempo
better than the best tracked browser baseline, or within +/-10% where exact
parity is not meaningful.

Baseline: GitHub Actions run `28992985395`, Chrome for Testing
`150.0.7871.115`, five iterations, 86 ranked categories.

## Baseline Gaps

- Tempo best/tied categories: `40/86`
- Explicit gaps to close: `46`
- Wall p95: Tempo `2268 ms`, best `973 ms`, gap `+1295 ms`
- Cold start: Tempo `1501 ms`, best `953 ms`, gap `+548 ms`
- Browser RSS p95: Tempo `977.5 MiB`, raw Chrome `932.4 MiB`, gap `+45.1 MiB`
- CPU p95: Tempo `2003 ms`, best `1635 ms`, gap `+368 ms`
- Web navigation p95: Tempo `54 ms`, best `36 ms`, gap `+18 ms`
- FCP p95: Tempo `68 ms`, best `48 ms`, gap `+20 ms`
- Observations p95: Tempo `4`, best `1`, gap `+3`
- Max observation tokens p95: Tempo `218`, best `38`, gap `+180`

## Experiment Log

| Commit | Experiment | Hypothesis | Proof Command | Result |
| --- | --- | --- | --- | --- |
| `a5c96b0` | Ledger setup | Make optimization history explicit before changing behavior. | n/a | Committed. |
| `22d7106` | Cheap post-action origin refresh | Replacing redundant post-action full observes with a cheap `window.location.href` origin refresh should reduce full `observations_p95`, agent-run wall time, and CPU while preserving origin policy. Falls back to the old journaled audit-only full observe path when location is unavailable. | `cargo test -p tempo-agent -- --nocapture`; `cargo test -p tempo-cli run_cdp_task -- --nocapture`; `cargo fmt --check`; `git diff --check`; direct local `tempo-cli run-cdp-task` against `checkout.html` | Local package tests passed. Direct run completed 3 actions with `observations=1`, `model_input_observations=1`, `total_model_input_tokens=27`, `agent_run_ms=350`, browser/web metrics available. Full cross-runner benchmark proof pending in CI because local `python3` is 3.9. |
| pending | Final page-state oracle | If post-action full observations are no longer part of the model-input path, the benchmark must verify final task success from the live page state rather than the last journaled observation. This keeps `observations` honest while preserving an independent checkout oracle like the Playwright/browser-use baselines. | `cargo fmt --check`; `python3 -m py_compile scripts/agent_browser_bench.py scripts/validate-agent-bench-artifacts.py`; `cargo test -p tempo-cli run_cdp_task -- --nocapture`; failed full gate artifact from run `28995643680` | Full gate `28995643680` failed because `tempo-cdp-agent` reported `success_rate 0.000`; every Tempo row had 3 applied steps and 1 observation, but `final_oracle.source=tempo-final-observation` read stale pre-action state. Patch emits `final_page_state` from `run-cdp-task`, uses it for Tempo success, validates that `tempo-run.json final_page_state` matches the metric row, and fails closed if `final_page_state` is missing. CI proof pending. |
| pending | Safe local benchmark environment | Browser metric tests should not inherit product/API credentials, OTLP exporters, tempod tokens, or remote threat-domain feeds from a developer shell. This keeps optimization runs isolated and aligned with the account/control-plane boundary: credentials must be explicit, not ambient. | `python3 -m py_compile scripts/agent_browser_bench.py scripts/validate-agent-bench-artifacts.py`; `OPENAI_API_KEY=unsafe-test python3 scripts/agent_browser_bench.py --smoke --chrome /bin/true --output-dir /tmp/tempo-safe-env-test` | Harness now rejects unsafe ambient env before launching Chrome and scrubs child-process envs unless `TEMPO_AGENT_BENCH_ALLOW_UNSAFE_HOST_ENV=1` is explicitly set. Negative safety check exits before browser launch with the expected refusal. |
| pending | Harden benchmark host boundary | Subagent audit found bypasses in `chrome --version`, in-process Playwright baselines, Docker-gate preflight/live-CDP paths, and absolute host cache/output paths. The benchmark should fail closed before touching host browsers, caches, or developer directories unless explicitly opted into unsafe mode. | `python3 -m py_compile scripts/agent_browser_bench.py scripts/validate-agent-bench-artifacts.py`; `bash -n scripts/linux-agent-gate.sh scripts/agent-browser-bench.sh`; `OPENAI_API_KEY=unsafe-test python3 scripts/agent_browser_bench.py --smoke --chrome /bin/true --output-dir /tmp/tempo-safe-env-test`; `python3 scripts/agent_browser_bench.py --smoke --chrome /bin/true --output-dir /Users/jadenfix/Desktop/tempo-unsafe-output-test`; `OPENAI_API_KEY=unsafe-test bash scripts/linux-agent-gate.sh --smoke` | Local checks passed. Python now scrubs `chrome --version` and Playwright baseline Chrome launches, rejects benchmark output outside repo `target/` or temp roots, and blocks a broader credential/telemetry/threat-domain env set. Docker gate now rejects unsafe ambient env before build/preflight/live-CDP and rejects absolute cache dirs outside the repo unless explicitly opted into unsafe mode. |
| pending | PSS/USS gap validator alignment | Full gate run `28997529297` produced valid five-iteration browser artifacts, but the independent artifact validator still recomputed the older RSS-only gap categories and rejected `agent-browser-bench-gaps.json`. The validator must include the same PSS/USS row fields and ranked categories as the generator. | `python3 scripts/validate-agent-bench-artifacts.py --output-dir /tmp/tempo-full-gate-28997529297/linux-agent-browser-bench-full --expected-iterations 5 --require-derived-artifacts`; `python3 -m py_compile scripts/validate-agent-bench-artifacts.py`; `git diff --check` | The downloaded failed-run artifact now validates locally. Run `28997529297` measured Tempo at `46/90` best/tied categories, `44` gaps, wall p95 `1125 ms`, cold start `1084 ms`, browser RSS p95 `973.6 MiB`, and 100% success. CI rerun pending with the validator fix. |
| pending | Playwright-like Chrome lifecycle profile | Newton's artifact audit showed Tempo's Chrome process keeps BFCache/RenderDocument/PaintHolding behavior enabled while Playwright baselines disable it; this likely contributes to Tempo's stable `Documents=4`, `Frames=4`, and high `LayoutCount=19`. Add an explicit benchmark-only profile to test lifecycle parity without changing production defaults. | `cargo fmt --check`; `cargo test -p tempo-engine-cdp playwright_lifecycle_launch_args_match_browser_baseline_lifecycle_flags`; `python3 -m py_compile scripts/agent_browser_bench.py scripts/validate-agent-bench-artifacts.py`; full Docker gate pending after PR #1 foundation passes. | Adds `TEMPO_CDP_BENCH_PLAYWRIGHT_LIFECYCLE_ARGS=1` as an opt-in that appends `--disable-back-forward-cache` and `--disable-features=PaintHolding,RenderDocument` to Tempo's CDP launch. Benchmark rows record `cdp_launch_profile` so artifacts identify whether the profile was enabled. Full Linux proof pending. |
| pending | CDP insertText Type dispatch | `chromiumoxide::Element::type_str` dispatches one keydown and one keyup CDP command per character, so the checkout email field pays about 34 input CDP commands. CDP provides `Input.insertText` to insert a whole text string in one command after focus. The profile sweep showed this cuts Type/action layout churn enough to promote it for semantic Type actions while retaining a key-event compatibility lane. | `cargo fmt --all --check`; `cargo test -p tempo-engine-cdp default_type_dispatch_uses_insert_text_unless_key_events_opt_in --lib`; `cargo check -p tempo-cli -p tempo-engine-cdp`; `python3 -m py_compile scripts/agent_browser_bench.py scripts/validate-agent-bench-artifacts.py`; `bash scripts/check-linux-agent-gate-mode.sh`; full Docker default/key-events gates pending. | Defaults semantic CDP `Action::Type` to `Input.insertText`, keeps `TEMPO_CDP_BENCH_INSERT_TEXT_TYPE=1` as a compatibility no-op, adds `TEMPO_CDP_KEY_EVENT_TYPE=1` plus a `key-events` benchmark profile for old per-character keyboard dispatch, and records effective `cdp_type_dispatch` in artifacts. Live Type semantics tests are now included in the live-CDP smoke lane before merge. |
| pending | DOM-equivalent AX summary cache | The checkout benchmark now does only four CDP snapshots, but each snapshot repeats the same capped AX lookups, producing `cdp_ax_partial_tree_count=20` in validated artifacts. Reusing only AX summaries that exactly match the raw DOM fallback should cut repeated `Accessibility.getPartialAXTree` calls without hiding accessibility-only labels, roles, or values. | `cargo fmt --all --check`; `cargo check -p tempo-engine-cdp`; `cargo test -p tempo-engine-cdp enrichment_ --lib`; full Docker agent-automation gate pending. | Adds a per-driver AX summary cache keyed by DOM hash, selector, and raw DOM fallback role/name/value. Cache entries are stored only when applying the AX summary would preserve the raw DOM fallback exactly; AX overlays and changed DOM hashes keep the existing CDP lookup path. Expected checkout impact is lower `cdp_ax_partial_tree_count`, browser CPU, and browser context switches while keeping snapshot count and success unchanged. |
| pending | CDP no-incognito fresh profile launch | Chromiumoxide does not expose a clean public browser-process kill handle, so bounded close/force-kill has weak testability. The lower-risk launch prototype is to remove the extra incognito BrowserContext only when explicitly opted in, relying on Tempo's already fresh temp `user_data_dir` for benchmark storage isolation. | `cargo fmt --check`; `cargo test -p tempo-engine-cdp default_launch_keeps_incognito_until_bench_no_incognito_opt_in`; `TEMPO_CDP_CHROME=/path/to/chrome cargo test -p tempo-engine-cdp live_cdp_bench_no_incognito_keeps_fresh_profile_storage_isolated -- --nocapture`; full Docker benchmark pending. | Adds `TEMPO_CDP_BENCH_NO_INCOGNITO=1`/`CdpConfig::with_bench_no_incognito()` as an opt-in and wires the CLI env path. Defaults still launch an incognito BrowserContext. Benchmark rows record `cdp_browser_context`; live proof asserts the benchmark profile is not incognito and that `localStorage` written in one fresh temp profile is absent from the next fresh temp profile. |
| pending | Remove duplicate request interception setup | Chromiumoxide's builder-level request interception enables protocol interception/cache commands, while Tempo already installs its own Fetch policy interception per page. Keeping both creates duplicate target-init CDP work before every benchmark run. | `cargo fmt --check`; `cargo test -p tempo-engine-cdp browser_config_leaves_request_interception_to_tempo_policy_layer -- --nocapture`; full Docker/live policy proof pending. | Removes `.enable_request_intercept()` from the CDP browser config while keeping the previous no-cache behavior and Tempo's explicit `install_request_policy` Fetch layer. Expected impact is small but real launch CPU/CDP-command reduction. |
| pending | Reuse current observation seq for actions | `tempo-act::execute_action` performed a full pre-action `driver.observe()` even when `AgentRunner` already had a current observation seq from initial navigation or resume. CDP then performed another full post-action observation to produce the action diff. Reusing the known base seq should remove one full DOM/AX snapshot per plain checkout action without changing post-action grounding. | `cargo fmt --check`; `cargo test -p tempo-act`; `cargo test -p tempo-agent`; full Docker proof pending. | Adds `execute_action_since(driver, action, base_seq)` and wires `AgentRunner` plain actions through it when a current observation seq is known. Existing `execute_action` behavior remains available for external callers. Expected checkout impact: remove three full internal pre-action snapshots before measuring further lean-observe work. |
| pending | Direct current-URL origin refresh | The scripted checkout path still paid one generic `Runtime.evaluate("window.location.href")` after each side-effect action even though CDP can expose the current URL directly. Add an optional `DriverTrait::current_url()` primitive so CDP refreshes origin with one policy-checked URL read, while non-CDP engines keep the JS/full-observe fallback. | `cargo fmt --check`; `cargo test -p tempo-driver current_url -- --nocapture`; `cargo test -p tempo-agent runner_ -- --nocapture`; `cargo test -p tempo-engine-cdp -- --nocapture`; full Docker benchmark pending. | CDP implements direct current URL refresh via the existing `enforce_current_url_policy()` guard. Agent tests assert the fast path does not call JavaScript when available and still records an audit observation when both URL refresh paths are unavailable. Expected impact is small but on the measured three-action checkout hot path. |
| pending | Remote benchmark optimization profiles | The lower-level optimizations are intentionally env-gated, but the GitHub full gate needed a first-class way to run honest Docker measurements with those envs enabled. | `bash -n scripts/linux-agent-gate.sh`; `bash scripts/check-linux-agent-gate-mode.sh`; full Docker profile runs pending. | Adds workflow dispatch `benchmark_profile` choices (`default`, `lifecycle`, `insert-text`, `no-incognito`, `cache`, `desktop`, `all`) and passes them through `scripts/linux-agent-gate.sh` to the relevant `TEMPO_CDP_BENCH_*` env vars. Pull request and scheduled gates keep `default`. |
| pending | Direct procfs memory sampler | The benchmark sampler shells out to `pgrep` and `ps` every 50ms, adding avoidable process churn and harness CPU while measuring short browser runs. Reading `/proc` directly should preserve RSS/PSS/USS/process attribution while reducing measurement overhead and noise. | `python3 -m py_compile scripts/agent_browser_bench.py scripts/validate-agent-bench-artifacts.py`; fake `TEMPO_PROC_ROOT` procfs sanity check for descendant traversal, RSS/process-type aggregation, cmdline parsing, and PSS/USS totals; full Docker proof pending. | Adds a Linux `/proc` fast path for descendant discovery, command/cmdline reads, RSS process snapshots, and `smaps_rollup` memory totals. The existing subprocess path remains as non-Linux fallback. Expected impact is benchmark overhead/noise reduction rather than product behavior change. |
| `53a9207` | Bounded AX enrichment selection | CDP observation should not sort every extracted interactive element just to choose the small AX enrichment cap. A bounded top-k selector should preserve highest-rank/document-order semantics while reducing observe CPU on large pages. | `cargo fmt --check`; `git diff --check`; subagent: `cargo test -p tempo-engine-cdp top_ranked_indices_picks_highest_ranks_in_document_order`; subagent: `cargo test -p tempo-engine-cdp --lib` | Replaces full-list sort in `top_ranked_indices` with a bounded selection buffer capped by `MAX_AX_ENRICHED_ELEMENTS` while preserving document-order output. Full Docker benchmark proof pending on a pushed head. |
| pending | Honest ranked metric contract | Subagent audit found the scoreboard ranked unknown CDP `Performance.getMetrics` fields and timestamp/constant Web Timing fields as lower-is-better, inflating best/tied counts. Keep raw metrics but rank only defensible fields, and add independent `max_process_count` instead of ranking only process count at RSS peak. | `python3 -m py_compile scripts/agent_browser_bench.py scripts/validate-agent-bench-artifacts.py scripts/agent_bench_status.py`; `git diff --check`; in-memory recompute of run `29001609608` with `max_process_count` fallback | New ranking contract keeps raw CDP/Web maps in artifacts, drops unknown CDP and non-actionable web fields from `categories`, adds `max_process_count_p95`, and updates status markdown. On run `29001609608` recomputed categories become `56` with Tempo best/tied `30/56` and `26` gaps. |
| `4b719bc` | Allow-all policy proxy direct connect | The trusted local benchmark sets `UrlPolicy::allow_all`, whose resolved-socket guard already intentionally returns allow. The CDP policy proxy still paid an explicit `lookup_host`/sort/dedup/resolved-socket loop before connecting. Skipping that loop only for `allow_all` should reduce trusted-fixture navigation/resource timing without weakening the default SSRF path. | `cargo fmt --check`; `git diff --check`; `cargo test -p tempo-net url_policy_allow_all_skips_resolved_socket_guard`; `cargo test -p tempo-engine-cdp policy_proxy_allow_all_forwards_http_loopback`; full Docker run `29005911063` on later head `aea7d11` | Adds `UrlPolicy::is_allow_all()` and a proxy fast path that connects directly only when URL policy is explicitly allow-all. Default `block_private` still performs pre-connect resolution and resolved-socket enforcement. Combined with `aea7d11`, full Docker all-profile improved wall p95 `828 -> 746 ms`, CPU p95 `766 -> 741 ms`, browser RSS p95 `1013497856 -> 1008316416`, FCP p95 `60 -> 56 ms`, and resource duration p95 `16 -> 7 ms`; best/tied stayed `31/56`, gaps stayed `25`. |
| `aea7d11` | Proxy-only trusted fixture request policy | The all-profile benchmark's `--allow-private-network` checkout fixture still installed CDP Fetch interception and paused every request even though Tempo's mandatory policy proxy already enforces `BrowserHardeningPolicy` for HTTP(S). Launching the trusted fixture with allow-all policy up front and skipping duplicate Fetch interception should reduce response/FCP/resource timing without removing the proxy or changing default launches. | `cargo fmt --check`; `git diff --check`; `cargo test -p tempo-engine-cdp cdp_config -- --nocapture`; `cargo test -p tempo-engine-cdp trusted_proxy_only_policy_requires_explicit_config -- --nocapture`; `cargo test -p tempo-engine-cdp policy_proxy_blocks_browser_hardening_risky_downloads -- --nocapture`; `cargo check -p tempo-cli`; full Docker all-profile run `29005911063` | Adds explicit `CdpConfig::with_allow_all_url_policy()` and `with_proxy_only_request_policy()` and wires only `tempo-cli run-cdp-task --allow-private-network` through them. Defaults keep block-private URL policy plus Fetch request interception; proxy hardening still blocks risky downloads under allow-all. Full artifact validates; remaining key gaps are browser RSS `+26005504`, PSS `+9802752`, USS `+7335936`, max process count `+1`, FCP `+8 ms`, resource response end `+13 ms`, and durable `max_observation_tokens_p95 +163`. |
| pending | Borrowed-key CDP action diffs | CDP action diffs cloned every `NodeId` string into before/after hash maps on each action. The shared `tempo-observe` diff already uses borrowed `&str` keys for this short-lived index; mirroring that should reduce allocator churn, CPU, and transient RSS on every action diff while preserving behavior. | `cargo fmt --check`; `git diff --check`; `cargo test -p tempo-engine-cdp computes_added_removed_and_changed_diffs -- --nocapture`; full Docker proof pending. | Replaces cloned `NodeId` hash-map keys with borrowed `&str` indexes and preserves document-order added/changed/removed output. |
| `2ba5039` | Benchmark cache parity profile | Tempo still forced `Network.setCacheDisabled(true)` while raw Chrome and Playwright baselines leave cache enabled by default. Add an explicit benchmark profile to test cache parity without changing production/local defaults. | `cargo fmt --check`; `git diff --check`; `cargo test -p tempo-engine-cdp benchmark_cache_profile_leaves_chrome_cache_enabled -- --nocapture`; `cargo test -p tempo-engine-cdp browser_config_leaves_request_interception_to_tempo_policy_layer -- --nocapture`; `bash scripts/check-linux-agent-gate-mode.sh`; `python3 -m py_compile scripts/agent_browser_bench.py`; full Docker all-profile run `29007895510` | Regression. With cache included in `all`, best/tied fell `31/56 -> 29/56` and gaps rose `25 -> 27`. Wall p95 regressed `746 -> 785 ms`, CPU p95 `741 -> 799 ms`, browser RSS `1008316416 -> 1011548160`, FCP `56 -> 76 ms`, and resource response end `55 -> 78 ms`. Keep standalone `cache` profile for future investigation, but remove it from `all`. Defaults still disable cache. |
| `5894b3c` | Remove cache profile from all benchmark profile | Since cache parity regressed the full profile, remove it from `all` while preserving a standalone `cache` profile for future investigation. | `cargo fmt --check`; `git diff --check`; `bash scripts/check-linux-agent-gate-mode.sh`; full Docker all-profile run `29010602046`; `python3 scripts/validate-agent-bench-artifacts.py --output-dir /tmp/tempo-full-gate-29010602046/linux-agent-browser-bench-full --expected-iterations 5 --require-derived-artifacts` | Full artifact validates. Best/tied recovered and improved to `32/56`, gaps fell to `24`. Tempo remained best for cold/wall/internal/CPU/model-input categories. Key remaining gaps: browser RSS `+22659072`, PSS `+13987840`, USS `+11694080`, max process count `+3`, FCP `+20 ms`, resource response end `+16 ms`, and durable `max_observation_tokens_p95 +163`. Artifacts show `xdg-mime`/`xdg-settings` in max-process-count peaks on 3/5 Tempo iterations and in RSS attribution on all iterations. |
| `0e6805b`/`cdfa057` | Suppress benchmark desktop integration helpers | Chrome's Linux desktop-integration probes spawn short-lived `xdg-mime`/`xdg-settings` helpers that inflate process-count peaks and add a few MiB of transient RSS without representing browser/agent work. Suppressing those only in the benchmark profile should reduce process count noise without changing production launches. | `cargo fmt --check`; `git diff --check`; `python3 -m py_compile scripts/agent_browser_bench.py scripts/validate-agent-bench-artifacts.py`; `bash scripts/check-linux-agent-gate-mode.sh`; `cargo test -p tempo-engine-cdp normalizes_duplicate_disable_features_launch_args -- --nocapture`; `cargo test -p tempo-engine-cdp desktop_suppression_profile_is_explicit_and_benchmark_only -- --nocapture`; `cargo clippy -p tempo-engine-cdp --all-targets -- -D warnings -D clippy::unwrap_used -D clippy::expect_used`; full Docker all-profile run `29011863941`; `python3 scripts/validate-agent-bench-artifacts.py --output-dir /tmp/tempo-full-gate-29011863941/linux-agent-browser-bench-full --expected-iterations 5 --require-derived-artifacts` | Mixed but net regression. The artifact validates and `processes_at_max_count` proves the spike source, but helper suppression did not fully remove `xdg-mime`/`xdg-settings`. It improved max process count `12 -> 10`, PSS `232094720 -> 229189632`, and USS `120303616 -> 118362112`, but regressed best/tied `32/56 -> 28/56`, wall p95 `758 -> 1042 ms`, CPU p95 `746 -> 793 ms`, FCP `60 -> 64 ms`, and resource response end `51 -> 56 ms`. Keep standalone `desktop` profile and `processes_at_max_count` diagnostics, but remove desktop suppression from `all`. |
| `d7e86b6` | Remove desktop suppression from all benchmark profile | Since desktop suppression improved process count but regressed wall/web categories and did not eliminate helper probes, keep it as a standalone diagnostic profile but restore the main `all` profile to the proven no-cache composition. | `cargo fmt --check`; `git diff --check`; `python3 -m py_compile scripts/agent_browser_bench.py scripts/validate-agent-bench-artifacts.py`; `bash scripts/check-linux-agent-gate-mode.sh`; full Docker all-profile runs `29012387277` and `29013107715`; `python3 scripts/validate-agent-bench-artifacts.py --output-dir /tmp/tempo-full-gate-29013107715/linux-agent-browser-bench-full --expected-iterations 5 --require-derived-artifacts` | Removes `TEMPO_CDP_BENCH_SUPPRESS_DESKTOP=1` from `all`; explicit `desktop` profile remains selectable. Run `29012387277` validated but had one Tempo outlier (`agent_run_ms=2411`, `driver_close_ms=1219`) that dominated p95. Confirmation run `29013107715` validated with stable Tempo iterations: best/tied `31/56`, gaps `25`, wall p95 `759 ms`, CPU p95 `766 ms`, browser RSS gap `+24244224`, PSS gap `+13663232`, USS gap `+11202560`, max process count gap `+6`, FCP gap `+12 ms`, resource response end gap `+12 ms`, durable observation gap `+163 tokens`. |
| pending | Stricter browser memory eval contract | Subagent audit found that `browser_rss_bytes_p95`/PSS/USS report Chrome memory at the process-tree peak, not each Chrome process-type's own peak, and that web resource count/byte totals are parity fields rather than defensible lower-is-better optimization categories. Keep all raw metrics visible while adding true browser peak memory categories and removing resource count/byte fields from ranking. | `python3 -m py_compile scripts/agent_browser_bench.py scripts/validate-agent-bench-artifacts.py scripts/agent_bench_status.py`; in-memory generator/validator parity recompute of full run `29013107715`; `git diff --check` | Generator and independent validator agree on the stricter contract. On the raw rows from run `29013107715`, categories remain `56`, but Tempo best/tied becomes `29/56` with `27` gaps because true browser peak memory is now ranked and web resource count/byte fields are no longer scored as lower-is-better. Resource count and byte totals remain in row/status artifacts as parity evidence. |
| pending | Current-thread run-cdp-task runtime profile | The benchmark `run-cdp-task` process creates a fresh multi-thread Tokio runtime for one short browser job. Test a benchmark-only current-thread runtime to cut thread startup and stack/RSS overhead without changing production defaults until full Linux metrics prove it. | `cargo fmt --check`; `cargo test -p tempo-cli run_cdp_task -- --nocapture`; `TEMPO_CDP_BENCH_CURRENT_THREAD_RUNTIME=1 cargo test -p tempo-cli run_cdp_task_private_structured_probe_returns_before_chrome_launch -- --nocapture`; `bash -n scripts/linux-agent-gate.sh`; `bash scripts/check-linux-agent-gate-mode.sh`; `python3 -m py_compile scripts/agent_browser_bench.py`; negative validator copy missing `tempo_runtime_flavor`; full Docker runtime-profile run `29016145264`; `python3 scripts/validate-agent-bench-artifacts.py --output-dir /tmp/tempo-full-gate-29016145264 --expected-iterations 5 --require-derived-artifacts` | Regression. The artifact validates and every Tempo row reports `tempo_runtime_flavor=current-thread`, but best/tied falls to `24/56` with `32` gaps. Wall p95 regresses `759 -> 817 ms`, internal wall `751 -> 809 ms`, CPU `766 -> 787 ms`, max RSS `1040781312 -> 1043611648`, PSS `256935936 -> 265677824`, and USS `144560128 -> 150171648`. It improves max process count `15 -> 10`, nav `44 -> 39 ms`, FCP `60 -> 52 ms`, and resource response end `58 -> 57 ms`, but not enough to offset the regression. Keep `runtime` as a standalone diagnostic profile; remove it from `all`. |
| local-uncommitted | Trusted fixture launch without policy proxy | Launching the `--allow-private-network` fixture without request interception, policy proxy, cache disable, or extra network-hardening flags might reduce CDP driver launch time and align Tempo's Chrome args with raw baselines. | Direct local `tempo-cli run-cdp-task` against `checkout.html` before committing. | Rejected for now. First run was worse (`driver_launch_ms=2217`, total `2668`), warm rerun returned to prior shape (`driver_launch_ms=883`, total `1331`) without proving a win. Code removed before commit; revisit only with full Linux benchmark isolation. |

## Candidate Experiments

1. Reuse action diffs for post-action audit observations when the diff is
   complete enough, avoiding redundant full `driver.observe()` calls.
2. Add trusted benchmark launch mode that skips policy proxy/interception when
   the fixture is explicitly private-network allowed.
3. Replace semantic type action's per-character CDP typing with a single DOM
   value assignment plus `input`/`change` events where policy permits.
4. Split durable audit observation size from comparable model-facing observation
   size so `max_observation_tokens_p95` reflects the optimized agent stream.
