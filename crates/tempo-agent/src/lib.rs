//! tempo-agent — durable agent loop: observe->decide->act, Anthropic client reuse, idempotency, token-budget manager
//!
//! Status: scaffold. See ../../final.md for the component contract and Definition of Done.

/// Placeholder so the crate compiles; replaced by the real implementation per final.md §8.
pub fn describe() -> &'static str {
    "durable agent loop: observe->decide->act, Anthropic client reuse, idempotency, token-budget manager"
}
