//! tempo-headless — headless agent daemon (tempod): axum HTTP + MCP + BiDi, session pool, engine-host supervision, OTLP
//!
//! Status: scaffold. See ../../final.md.

fn main() {
    println!("tempo-headless: scaffold — see final.md for the design");
}
