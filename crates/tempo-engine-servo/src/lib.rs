//! tempo-engine-servo — libservo embedding: WebView + delegate, offscreen paint, screenshot, js-eval, input, request interception, AccessKit intake
//!
//! Status: scaffold. See ../../final.md for the component contract and Definition of Done.

/// Placeholder so the crate compiles; replaced by the real implementation per final.md §8.
pub fn describe() -> &'static str {
    "libservo embedding: WebView + delegate, offscreen paint, screenshot, js-eval, input, request interception, AccessKit intake"
}
