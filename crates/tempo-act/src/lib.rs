//! tempo-act — action executor: NodeId->injection, batching, quiescence detector, grounding-verify via post-action diff
//!
//! Status: scaffold. See ../../final.md for the component contract and Definition of Done.

/// Placeholder so the crate compiles; replaced by the real implementation per final.md §8.
pub fn describe() -> &'static str {
    "action executor: NodeId->injection, batching, quiescence detector, grounding-verify via post-action diff"
}
