//! tempo-session — session lifecycle, journal (beater-agent), cassettes/replay (beater-replay), deterministic re-execution
//!
//! Status: scaffold. See ../../final.md for the component contract and Definition of Done.

/// Placeholder so the crate compiles; replaced by the real implementation per final.md §8.
pub fn describe() -> &'static str {
    "session lifecycle, journal (beater-agent), cassettes/replay (beater-replay), deterministic re-execution"
}
