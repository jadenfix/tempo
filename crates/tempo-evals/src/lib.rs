//! tempo-evals — WebVoyager/WebArena/Mind2Web-Live adapters over beater-eval/judge; latency+token budgets as evaluators
//!
//! Status: scaffold. See ../../final.md for the component contract and Definition of Done.

/// Placeholder so the crate compiles; replaced by the real implementation per final.md §8.
pub fn describe() -> &'static str {
    "WebVoyager/WebArena/Mind2Web-Live adapters over beater-eval/judge; latency+token budgets as evaluators"
}
