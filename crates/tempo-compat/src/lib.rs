//! tempo-compat — nightly top-1k scorecard, per-origin lane table, fallback-rate KPI, injection red-team corpus runner
//!
//! Status: scaffold. See ../../final.md for the component contract and Definition of Done.

/// Placeholder so the crate compiles; replaced by the real implementation per final.md §8.
pub fn describe() -> &'static str {
    "nightly top-1k scorecard, per-origin lane table, fallback-rate KPI, injection red-team corpus runner"
}
